import React from 'react';

import './Timer.css';

const Timer = () => {
    return (
        <div className="timer">
            <h1 className="timer-content">TIMER</h1>
        </div>
    );
};

export default Timer;
